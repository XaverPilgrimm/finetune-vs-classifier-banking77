# Fine-Tuning vs. API Benchmark

Comparing LoRA fine-tuning an open-weight LLM (Qwen2.5-7B-Instruct) against
prompting a hosted API model (GPT-4o-mini) on the same task: AG News topic
classification.
